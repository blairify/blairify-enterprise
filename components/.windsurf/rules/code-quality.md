---
trigger: always_on
---

Check if the code written does not add unnecessary complexity which could be resolved with es-toolkit. 
Clean practice `h-4 w-4` switch it to `size-4`
Colocate types with usage
Minimal comments (code self-documents)
Single responsibility functions
Early returns over nesting
